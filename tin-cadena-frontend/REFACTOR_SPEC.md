# Tin Cadena Gallery Refactor Spec

- [x] Task 1: Design Tokens & Base Styles [ `tailwind.config.cjs`, `src/styles/globals.css` ]
- [x] Task 2: High-Impact Core Components [ `RoomView.tsx`, `VariantConfigurator.tsx` ]
- [x] Task 3: Catalog & Exhibition Grid [ `src/pages/Explore.tsx` and Photo Card ]
- [x] Task 4: Visual Polish, Accessibility & Build QA [ `npm run build` ]

---

## Directives
1. Execute only ONE task at a time.
2. After finishing a task, run `npm run build` - or Vite check - to ensure zero TypeScript/CSS errors.
3. Once validated, update this document marking the task as [x].
4. Immediately proceed to the next unchecked task without asking for permission unless blocked by an unresolvable error.